spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cantor
    username: cantor
    password: cantor
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
